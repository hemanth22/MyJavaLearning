build:
    environment:
        docker: true
            
            
tests:
     override:
         - command: docker run --rm -v"$PWD":/usr/src/myapp -w /usr/src/myapp java:7 javac -d . JavProg.java
           on_node: 1


