version: 2
jobs:
     build:
       docker:
         - image: java:7
       steps:
         - checkout
         - docker run --rm -v"$PWD":/usr/src/myapp -w /usr/src/myapp java:7 javac -d . HelloWorld.java
         - echo "Listing class files"
         - ls -la
         - echo "Output of java file"
         - docker run --rm -v"$PWD":/usr/src/myapp -w /usr/src/myapp java:7 java learnjava.HelloWorld
